interface:
  display_name: "Demo Video"
  short_description: "Record verified demos of runnable applications"
  default_prompt: "Use $demo-video to understand this repository, run its executable applications locally, and create a captioned demo video for each in docs/demo/."
policy:
  allow_implicit_invocation: true
